section.request__wrap
  h2.hidden Свяжитесь с нами
  button.close(tabindex="-1")
    img(src=require("../../images/cross-big.svg"), alt="Закрыть" loading="lazy") 
  form
    fieldset
      legend.title Мы обязательно вам ответим!
      ul.request__list 
        li.request__item 
          label.hidden(for="req_name") Имя Фамилия
          input#req_name(type="text" placeholder="Имя Фамилия" tabindex="-1" required)
        li.request__item 
          label.hidden(for="req_email") E-mail
          input#req_email(type="email" placeholder="email@example.com" tabindex="-1" required)
        li.request__item 
          label.hidden(for="req_message") Сообщение
          textarea#req_message(cols="50" rows="5" placeholder="В свободной форме" tabindex="-1" required) 
    button.btn_medium(tabindex="-1") Отправить